function mostrar()
{let numero;
    let maximo;
	let minimo; 

	maximo=10;
	minimo=1;


	numero=parseInt(Math.round(Math.random() * (maximo - minimo)+ minimo ));


	alert(numero);	

	if (numero >8 && 10)
	{

		alert ("EXCELENTE")
	}
	if (numero >4 && 8)
	{
	alert ("APROBO");
}
if (numero <4)
{
alert ("Vamos, la próxima se puede");

}
}